After the make has been compiled by `make` , run it with the following command (using huge.fa as an example)

`mpirun -np 8 . /cdhit -i huge.fa -o test`

If there are other parameters you can keep them the same as cdhit.

Working Logs:
